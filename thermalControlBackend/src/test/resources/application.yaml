spring:
  main:
    banner-mode: "off"
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  sql:
    init:
      mode: always
      data-locations: classpath:init.sql
logging:
  level:
    org: "ERROR"
    root: "ERROR"